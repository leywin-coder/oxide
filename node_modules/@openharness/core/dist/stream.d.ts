import type { AgentEvent } from "./agent.js";
type StreamTransform = (source: AsyncIterable<AgentEvent>) => AsyncGenerator<AgentEvent>;
/** Observe events without modifying the stream. */
export declare function tap(fn: (event: AgentEvent) => void): StreamTransform;
/** Drop events that don't match the predicate. Never filters `done` events. */
export declare function filter(predicate: (event: AgentEvent) => boolean): StreamTransform;
/** Transform each event. */
export declare function map(fn: (event: AgentEvent) => AgentEvent): StreamTransform;
/** Stop iteration after predicate matches (inclusive — the matching event is yielded). */
export declare function takeUntil(predicate: (event: AgentEvent) => boolean): StreamTransform;
export {};
//# sourceMappingURL=stream.d.ts.map