import { type TodoItem, type TodoStore, type TodoUpdate } from "../todos.js";
export interface CreateTodoToolsOptions {
    /** Stable session key for todo persistence. Defaults to "default" for simple single-session usage. */
    sessionId?: string;
    /** Pluggable persistence. Defaults to an in-memory store scoped to this tool factory call. */
    store?: TodoStore;
    /** Called after a successful write. Useful for app-specific event buses or logs. */
    onUpdate?: (update: TodoUpdate) => void | Promise<void>;
}
export declare function createTodoTools(options?: CreateTodoToolsOptions): {
    todowrite: import("ai").Tool<{
        todos: {
            content: string;
            status: "pending" | "in_progress" | "completed" | "cancelled";
            priority: "high" | "medium" | "low";
            id?: string | undefined;
        }[];
    }, {
        sessionId: string;
        todos: TodoItem[];
    }>;
    todoread: import("ai").Tool<Record<string, never>, {
        sessionId: string;
        todos: TodoItem[];
    }>;
};
//# sourceMappingURL=create-todo-tools.d.ts.map