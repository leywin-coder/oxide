export class InMemorySubagentSessionMetadataStore {
    entries = new Map();
    async load(sessionId) {
        return this.entries.get(sessionId);
    }
    async save(metadata) {
        this.entries.set(metadata.sessionId, metadata);
    }
    async delete(sessionId) {
        this.entries.delete(sessionId);
    }
    async list(filter) {
        const values = [...this.entries.values()];
        if (!filter?.agentName)
            return values;
        return values.filter((entry) => entry.agentName === filter.agentName);
    }
}
export function isSubagentCatalog(source) {
    return !Array.isArray(source);
}
//# sourceMappingURL=subagents.js.map