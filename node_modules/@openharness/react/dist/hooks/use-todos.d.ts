import { type TodoState } from "../context.js";
export interface UseTodosResult extends TodoState {
    total: number;
    pending: number;
    inProgress: number;
    completed: number;
    cancelled: number;
    hasTodos: boolean;
    isComplete: boolean;
    activeTodo: TodoState["todos"][number] | undefined;
}
/**
 * Derives the latest todo state from `data-oh:todo.updated` events.
 *
 * Must be used within an `<OpenHarnessProvider>`.
 */
export declare function useTodos(): UseTodosResult;
//# sourceMappingURL=use-todos.d.ts.map